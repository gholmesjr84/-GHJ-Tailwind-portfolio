import React from 'react'
import data from '../content/site.json'
export default function Services(){return(<section id="services" className="py-12 bg-white/60 dark:bg-neutral-900/50" aria-labelledby="services-heading"><div className="max-w-6xl mx-auto px-4"><div className="text-center mb-6"><h2 id="services-heading" className="logo-font text-3xl text-deepplum dark:text-mustard">Commissions</h2><p className="text-neutral-600 dark:text-neutral-300">Accessible pricing while I ramp back up — quality, clarity, and honest scope.</p></div><div className="grid gap-4 md:grid-cols-3">{data.tiers.map(t=>(<div key={t.name} className="card p-6"><div className="flex items-center justify-between"><h3 className="text-xl font-semibold">{t.name}</h3>{t.badge&&<span className="rounded-full bg-mustard/90 text-black text-xs px-2 py-1">{t.badge}</span>}</div><p className="mt-2 text-3xl text-deepplum dark:text-mustard">{t.price}</p>{t.blurb&&<p className="mt-1 text-neutral-600 dark:text-neutral-300">{t.blurb}</p>}<ul className="mt-2 list-disc list-inside space-y-1">{t.features?.map(f=><li key={f}>{f}</li>)}</ul>{t.addons&&<p className="mt-2 text-sm text-neutral-600 dark:text-neutral-300"><strong>Add‑ons:</strong> {t.addons.join(' · ')}</p>}<a href="#contact" className="btn-brand mt-3" aria-label={`Start a ${t.name} commission`}>Start a commission</a></div>))}</div><p className="text-center text-sm text-neutral-600 dark:text-neutral-300 mt-4">Prices are starting points. Commercial usage and rush timelines quoted separately.</p></div></section>)}
